<%= form_with(model: daily_exsp_detail, local: true) do |form| %>
  <% if daily_exsp_detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(daily_exsp_detail.errors.count, "error") %> prohibited this daily_exsp_detail from being saved:</h2>

      <ul>
        <% daily_exsp_detail.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

 

<table class="table">
  <tr>
    <td> <%= form.label :user_id %></td>
    <td><%= form.number_field :user_id , class:'form-control'%></td>
  </tr>
  <tr>
    <td><%= form.label :daily_exspenses_id %></td>
    <td><%= form.number_field :daily_exspenses_id , class:'form-control'%></td>
  </tr>
  <tr>
    <td><%= form.label :amount %></td>
    <td><%= form.number_field :amount,class:'form-control' %></td>
  </tr>
  <tr>
    <td> <%= form.label :is_paid %></td>
    <td>  <%= form.check_box :is_paid ,class:'form-control'%></td>
  </tr>
  <tr>
    <td colspan="2" class="btn btn-success"><%= form.submit %></td>
  </tr>

</table>

<% end %>