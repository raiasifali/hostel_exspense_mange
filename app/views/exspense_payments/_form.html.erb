<%= form_with(model: exspense_payment, local: true) do |form| %>
  <% if exspense_payment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(exspense_payment.errors.count, "error") %> prohibited this exspense_payment from being saved:</h2>

      <ul>
        <% exspense_payment.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :paid_amount %>
    <%= form.number_field :paid_amount %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
